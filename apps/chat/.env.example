# Hermes Agent OpenAI 兼容后端
# 默认指向本地 hermes (NousResearch/hermes-agent)
VITE_HERMES_URL=http://localhost:8642/v1

# 模型名 · hermes 默认 'hermes' · 后端忽略也无所谓
VITE_HERMES_MODEL=hermes

# API key · hermes 无 auth · 任意非空字符即可 (openai SDK 要求非空)
VITE_HERMES_API_KEY=hermes-local
